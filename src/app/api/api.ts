// import type { NextApiRequest, NextApiResponse } from 'next';

// export default function POST(req: NextApiRequest, res: NextApiResponse) {
//     if (req.method === 'POST') {
//         const { plate } = req.body;

//         // Lógica para spawnar o veículo com a placa fornecida

//         res.status(200).json({ message: `Veículo com placa ${plate} spawnado com sucesso.` });
//     } else {
//         res.status(405).end();
//     }
// }
